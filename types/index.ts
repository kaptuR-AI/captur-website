export type IUserStep = {
    id: number,
    image: string,
    title: string,
    paragraph: string
}

export type Testimonial = {
    text: string;
    imageUrl: string;
    name: string;
    role: string;
};